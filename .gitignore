# Rust 构建产物
/target

# 运行时日志
*.log

# blazehttp 测试样本(GPL-3.0,不随本仓库分发;从 https://github.com/chaitin/blazehttp 获取)
/testdata/blazehttp

# 本地实际配置(如含私有源站/端点,可改用 config.example.toml 提交模板)
# config.toml

# 缺口捕获训练产物
/gaps.jsonl

# ML 原型产物(模型权重 / Python 缓存)
/ml/*.npz
/ml/__pycache__

# learn 工具产出(待审候选,可再生)
/candidate_rules.txt

# SQLite 运行时数据库(含封禁/直通/审计数据,不入库)
/limen.db
/limen.db-wal
/limen.db-shm
*.db
*.db-wal
*.db-shm
